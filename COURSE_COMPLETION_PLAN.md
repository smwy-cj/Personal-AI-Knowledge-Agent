# AI4SE 期末项目完善总方案

## 1. 方案目标

本方案用于将 `Personal-AI-Knowledge-Agent` 整理为满足 AI4SE B 类应用项目要求的可提交作品。

完善工作的重点不是继续横向增加大量功能，而是补齐课程验收所需的规格、过程证据、安全凭据管理、WebUI、分发、CI/CD 和线上演示闭环。

本轮工作忽略 Superpowers 的安装、技能调用和流程合规性，不把它们纳入实现任务或验收范围。

## 2. 文档保护原则

从本方案创建之日起，仓库中已有文档均视为只读历史材料，包括但不限于：

- `README.md`
- `docs/IMPLEMENTATION_PLAN.md`
- `docs/PROJECT_STATUS.md`
- `Personal_AI_Knowledge_Agent_Agent_Architecture_Design.md`
- `Personal_AI_Knowledge_Agent_架构深度评估与工程化建议.md`

后续不得直接修改、覆盖、删除或重命名上述文档。需要更新或纠正的内容一律写入新文件。

新文档采用以下规则：

- 课程明确指定文件名的交付物，直接创建对应的新文件。
- README 的课程提交版本使用 `README_COURSE.md`，保留原 `README.md`。
- 架构、部署、安全等专题材料放入新目录 `docs/course/`。
- 文档发生重要迭代时创建带版本后缀的新文件，例如 `SPEC_v2.md`，不覆盖旧版本。
- 所有回溯性过程记录必须标注证据来源和不确定性，不虚构历史对话、TDD 红灯或人工决策。

## 3. 项目定位

### 3.1 一句话说明

该项目是一个面向 Obsidian 用户的受治理个人知识智能体：它能够摄取本地知识库、执行带引用的混合检索与研究任务、生成受验证的摘要，并通过人工审批控制长期记忆写回。

### 3.2 目标用户

- 使用 Obsidian 管理大量笔记的学生、研究者和知识工作者。
- 需要可追溯引用，而不是只接受不可核验回答的用户。
- 重视本地数据边界、成本控制和长期记忆治理的用户。

### 3.3 B 类项目中的 Agent 定位

项目按“包含 Agent 部分的 B 类应用”准备，但避免宣称完全自主 Agent。

建议使用的准确表述是：

> 受治理的确定性 Agent Workflow。模型负责受约束的研究摘要等推理任务；项目自身实现任务编排、状态转换、工具执行、预算、重试、取消、持久化、验证和人工审批边界。

课程材料中必须区分：

- 模型推理；
- 确定性工作流；
- 工具执行；
- 治理与安全护栏；
- 人工审批节点。

## 4. 目标交付结构

计划新增以下文件，不修改原有文档：

```text
Personal-AI-Knowledge-Agent/
├── SPEC.md
├── PLAN.md
├── SPEC_PROCESS.md
├── AGENT_LOG.md
├── REFLECTION_GUIDE.md
├── README_COURSE.md
├── COURSE_COMPLETION_PLAN.md
├── .gitlab-ci.yml
├── Dockerfile
├── docker-compose.example.yml
├── .env.example
├── LICENSE
├── docs/
│   └── course/
│       ├── ARCHITECTURE.md
│       ├── SECURITY_AND_CREDENTIALS.md
│       ├── DISTRIBUTION_AND_DEPLOYMENT.md
│       ├── CLEAN_MACHINE_VERIFICATION.md
│       ├── COLD_START_VALIDATION.md
│       ├── TEST_EVIDENCE.md
│       ├── CI_CD_EVIDENCE.md
│       ├── PR_AND_REVIEW_EVIDENCE.md
│       └── DEMO_GUIDE.md
└── web/
    └── ...
```

说明：`REFLECTION.md` 必须由学生本人完成，因此先创建 `REFLECTION_GUIDE.md`，只提供问题框架、证据索引和写作提示，不代写最终反思。

## 5. 分阶段完善计划

### 阶段 0：建立不可变基线

目标：固定当前项目状态，避免后续材料把现有能力和新增能力混淆。

任务：

1. 记录当前分支和基线 commit。
2. 运行 `python scripts/verify.py` 并保存测试结果摘要。
3. 建立当前功能、测试、文档、CI 和分发资产清单。
4. 扫描 Git 跟踪文件和历史中的疑似密钥。
5. 明确哪些结论来自代码、哪些来自本地测试、哪些仍需线上验证。

验收标准：

- 149 项现有测试继续通过。
- 生成 `docs/course/TEST_EVIDENCE.md`。
- 没有把未验证的 GitHub/GitLab 在线状态写成事实。

### 阶段 1：补齐课程规格与计划

目标：形成可独立理解、可客观验收的课程规格。

#### 1.1 `SPEC.md`

必须包含：

1. 问题陈述、目标用户和价值。
2. 至少 5 个符合 INVEST 原则的用户故事。
3. 按模块描述输入、行为、输出、边界条件和错误处理。
4. 性能、安全、可用性和可观测性要求。
5. 组件图、数据流和外部依赖。
6. 主要实体、字段、关系与约束。
7. 凭据录入、保存、更新、状态查看和清除流程。
8. 技术选型及理由。
9. 每项功能的客观验收标准。
10. 风险、非目标和未决问题。
11. Agent/工作流边界及无真实 LLM 的确定性测试策略。

#### 1.2 `PLAN.md`

每个任务必须包含：

- 目标；
- 新增或修改的代码文件；
- 新增的课程文档；
- 首先应失败的测试；
- 最小实现；
- 验证命令；
- 依赖任务；
- 是否可以并行；
- 完成状态和 commit hash。

#### 1.3 `SPEC_PROCESS.md`

只记录可证明的过程：

- 需求澄清的关键问题；
- 至少 3 轮真实规格迭代；
- 被采纳、修改和拒绝的建议；
- 陌生智能体冷启动验证；
- 暴露的规格歧义；
- 修订前后的关键差异。

验收标准：陌生读者只阅读 `SPEC.md` 和 `PLAN.md`，无需口头补充即可选择一个任务开始实现。

### 阶段 2：完成凭据生命周期

目标：从“仅支持环境变量”升级为满足课程要求的安全凭据管理。

建议新增能力：

```text
personal-ai-agent credential set <provider-id>
personal-ai-agent credential status <provider-id>
personal-ai-agent credential delete <provider-id>
```

实现要求：

- `set` 使用隐藏输入。
- 优先写入系统密钥环。
- `status` 只返回是否存在和来源，不显示明文。
- `delete` 明确删除结果。
- 更新操作覆盖同一 Provider 的旧凭据。
- 环境变量仅作为兼容后备来源。
- 日志、异常、事件和测试输出均不得包含明文凭据。
- 无图形密钥环或服务器环境下必须有明确失败提示或安全替代方案。

测试要求：

- 使用内存 fake keyring，不访问开发者真实密钥环。
- 测试录入、覆盖、状态、删除、缺失后端和脱敏错误。
- 测试优先级规则：系统密钥环与环境变量谁优先必须在 SPEC 中固定。

### 阶段 3：增加最小可交付 WebUI

目标：满足线上 WebUI 要求，同时复用现有业务层。

#### 3.1 最小页面

- 首页：项目价值、知识库状态和快速入口。
- 搜索页：查询、过滤、引用结果。
- Research 页：提交任务、查看状态、结果和引用。
- Memory Review 页：查看待审批记忆并批准或拒绝。
- Settings 页：Provider 凭据状态，不显示密钥。
- Operations 页：成功率、延迟和估算成本摘要。

#### 3.2 WebUI 边界

- 浏览器不得持久化 Provider key。
- 前端不得直接调用付费模型 Provider。
- Web API 必须调用现有 Application Service，而不是复制领域逻辑。
- 写入 Obsidian 和批准 Memory 必须有显式确认。
- 线上演示只能使用脱敏示例 Vault。
- 错误页面不得展示堆栈、路径、Prompt 或 Provider 响应正文。

#### 3.3 建议技术路线

建议使用轻量 Python Web 框架配合服务器渲染页面，减少引入独立前端构建链的成本。最终选型在 `SPEC.md` 中说明理由。

验收标准：

- 陌生用户在 30 秒内理解用途。
- 可以从 WebUI 完成一次搜索。
- 可以查看带来源引用的结果。
- 可以完成一次 Memory 审批演示。
- 公开 URL 可访问，且不依赖个人真实 Vault。

### 阶段 4：完成可重复分发

目标：让助教能在全新机器上从零运行。

建议主路线为 Docker，Python 包作为辅助路线。

Docker 验收：

```text
docker build ...
docker run ...
```

要求：

- 构建过程不需要真实 key。
- 运行时通过安全机制提供凭据。
- 容器使用非 root 用户。
- 数据目录和示例 Vault 使用明确挂载点。
- 提供健康检查。
- 镜像不包含开发者数据、数据库、缓存或凭据。
- 推送到公开 registry，并记录不可变版本标签。

Python 包辅助验收：

- 从构建产物安装成功；
- `personal-ai-agent --help` 可运行；
- 明确 Python、操作系统和架构限制；
- 补齐许可证和包元数据。

### 阶段 5：补齐 GitLab CI/CD

目标：满足课程指定 CI 形态。

新增 `.gitlab-ci.yml`，至少包含名为 `unit-test` 的 job。

建议流水线：

1. `unit-test`：运行全部离线测试。
2. `package-build`：构建 Python wheel/sdist。
3. `container-build`：构建容器镜像。
4. `security-check`：检查跟踪文件中的疑似凭据和危险配置。
5. `deploy-demo`：仅在受保护分支和正确凭据条件下部署演示站点。

要求：

- 测试不能调用真实付费 API。
- CI key 只能保存在平台秘密变量中。
- 日志不得输出 secret。
- 最后一次提交对应的流水线必须通过。
- 生成 `docs/course/CI_CD_EVIDENCE.md`，记录流水线 URL、commit 和结果。

### 阶段 6：冷启动、PR 与评审证据

目标：补齐可审计的工程过程。

冷启动验证：

- 使用与主开发智能体不同类型的新会话。
- 只提供当时版本的 `SPEC.md` 和 `PLAN.md`。
- 指定 1 至 2 个尚未实现的任务。
- 遇到歧义时停止提问，不允许自行猜测。
- 记录问题、误读、产出差距和规格修订。

PR 证据：

- 后续每个独立整改模块使用独立分支和 PR。
- PR 说明列出 AI 生成部分、人工修改和测试证据。
- 先检查规格符合性，再检查代码质量。
- 严重问题修复后才合并。
- 不虚构早期不存在的 PR 或 worktree。

### 阶段 7：课程提交包装

目标：形成助教可按顺序验收的入口。

`README_COURSE.md` 应包含：

1. 30 秒项目介绍。
2. 目标用户和实际问题。
3. 功能模块。
4. 架构图。
5. 在线演示 URL。
6. 本地安装与运行。
7. Docker 获取与运行。
8. 凭据安全配置。
9. 一键测试。
10. CI/CD 链接。
11. 目录结构。
12. 安全和隐私边界。
13. 已知限制。
14. 第三方依赖及许可证。

`REFLECTION_GUIDE.md` 应整理真实证据并提示学生本人回答：

- TDD 对开发效率与质量的影响；
- 任务粒度是否合适；
- 规格歧义如何导致实现偏差；
- 哪些 AI 建议被采纳或推翻；
- 最有效的 prompt/context 策略；
- 凭据和分发要求暴露了哪些原先忽略的问题；
- 如果重做会改变什么；
- 对 Agentic SE 方法的批判性判断。

## 6. 推荐任务拆分

| 编号 | 任务 | 依赖 | 主要产出 |
|---|---|---|---|
| T0 | 固定基线与证据清单 | 无 | 测试及仓库基线证据 |
| T1 | 编写课程 SPEC | T0 | `SPEC.md` |
| T2 | 编写细粒度 PLAN | T1 | `PLAN.md` |
| T3 | 凭据安全存储 | T1 | keyring 抽象、CLI、测试 |
| T4 | Web API 边界 | T1 | Web application service adapter、测试 |
| T5 | WebUI 核心页面 | T4 | 搜索、Research、Memory 页面 |
| T6 | Web 安全与错误处理 | T4 | 安全 headers、脱敏错误、测试 |
| T7 | Docker 分发 | T3、T5 | Dockerfile、运行说明 |
| T8 | GitLab CI | T7 | `.gitlab-ci.yml` |
| T9 | 线上演示部署 | T7、T8 | URL、部署证据 |
| T10 | 冷启动验证 | T1、T2，且应在相应实现前 | 验证记录、规格修订 |
| T11 | PR/评审证据整理 | T3 至 T9 | 评审证据文档 |
| T12 | 课程 README | T3 至 T11 | `README_COURSE.md` |
| T13 | 反思写作指南 | T11 | `REFLECTION_GUIDE.md` |
| T14 | 最终干净机器验收 | 全部 | 最终验收记录 |

并行建议：

- T3 与 T4 可在 SPEC 稳定后并行。
- T6 可与 T5 的非重叠页面工作并行。
- T8 可先搭建测试 job，但镜像和部署 job 依赖 T7。
- 文档证据应随任务同步新增，不能全部拖到最后回忆。

## 7. 最终验收清单

### 7.1 功能

- 至少三个职责清晰的功能模块。
- 搜索、研究、记忆审批形成可演示闭环。
- WebUI 可通过公网 URL 访问。
- Agent 主循环和治理机制可在无真实 LLM 条件下确定性测试。

### 7.2 安全

- Git 当前树和历史中无真实凭据。
- key 可隐藏录入、更新、查看状态和清除。
- 配置、日志、错误和事件不回显 key。
- WebUI 不接收或持久化明文 key，或仅通过明确的服务器端安全录入流程处理。
- 演示数据全部脱敏。

### 7.3 测试与 CI

- 一条命令运行全部核心测试。
- 现有 149 项测试持续通过。
- 新增凭据、Web、容器边界相关测试。
- `.gitlab-ci.yml` 包含 `unit-test` job。
- 最后一次 CI/CD 为通过状态。

### 7.4 分发

- Docker 或包管理器分发至少完成一种。
- 在干净环境从零安装并启动成功。
- README 写明平台、架构、依赖和限制。
- 公开发行地址可用。

### 7.5 过程与仓库

- 指定课程文档齐全。
- 后续独立模块有分支、PR、评审和测试证据。
- `PLAN.md` 记录完成状态与 commit hash。
- NJU Git 仓库可供助教访问。
- 不存在伪造的历史过程记录。

## 8. 范围控制

提交前不建议继续增加以下内容，除非验收测试证明它们是阻塞项：

- 更多 LLM Provider；
- 更多计费格式；
- 更复杂的多 Agent 协作；
- 大规模前端设计系统；
- 移动端应用；
- 与课程验收无直接关系的新评测指标。

优先级始终为：

```text
课程硬性要求 > 安全与可运行性 > 演示闭环 > 工程证据 > 新功能数量
```

## 9. 建议的下一步

下一步先执行 T0 和 T1：生成不可变基线证据，并从当前代码事实出发创建全新的 `SPEC.md`。在 SPEC 经过人工确认前，不开始凭据管理和 WebUI 实现。
