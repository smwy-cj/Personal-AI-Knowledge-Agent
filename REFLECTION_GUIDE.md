# REFLECTION.md 学生本人写作指南

## 使用边界

课程要求 `REFLECTION.md` 为 1500–2500 字，并且必须由学生本人撰写。本文只提供可核验事实、问题清单和自查标准，不提供可以直接提交的反思正文，也不会替学生虚构使用 Superpowers、subagent、PR 或人工决策的经历。

建议先关闭本文，凭自己的记忆写出第一稿；再用下面的证据索引核对事实。若使用 AI 做语言润色，应按课程要求在正文末尾标注范围，并确保观点、判断和经历仍来自本人。

## 建议结构与字数

| 部分 | 建议字数 | 要回答的核心问题 |
|---|---:|---|
| 1. 项目与个人责任 | 150–250 | 为什么选择个人知识 Agent；你本人做了哪些选择 |
| 2. 规约与计划 | 250–350 | SPEC / PLAN 如何改变实现；哪里仍不清楚 |
| 3. TDD 与验证 | 250–350 | 红—绿—重构何时有价值、何时增加成本 |
| 4. 智能体协作与任务拆分 | 250–350 | subagent 或替代流程能自主多久；什么粒度合适 |
| 5. Prompt / context 策略 | 200–300 | 哪类上下文最有效；如何防止越界和虚构完成 |
| 6. 凭据、分发与部署 | 250–350 | 工程要求迫使你补上了什么 |
| 7. 方法论批判与重做 | 250–400 | Superpowers 的假设是否成立；如果重做会改变什么 |

总字数不必机械平均，但最终应在 1500–2500 字范围内，并用具体事件支撑判断。

## 必答问题清单

### Superpowers 与流程偏离

本项目按用户决定没有使用 Superpowers。不要写成“已完整遵循”。你需要本人说明：

- 为什么选择忽略它，当时的现实约束是什么？
- 哪些纪律仍通过替代方式保留，例如版本化 SPEC、细任务、失败测试、阶段证据、干净环境验证？
- 哪些课程目标因此没有充分满足，例如技能调用记录、worktree/每任务 PR、标准两阶段评审？
- 这项偏离对项目成绩、证据可信度和你的学习造成了什么影响？
- 如果重做，会安装并使用哪些 Superpowers 技能，哪些仍会保留人工判断？

避免只说“形式大于实质”。应区分：某个步骤本身无价值，还是本项目因为已有代码、工具不可用或时间顺序而没能正确使用它。

### TDD

至少写一个真实红—绿案例和一个失败测试帮你发现设计问题的案例。可核对：

- 凭据生命周期先建立安全行为测试，再实现 keyring adapter 和 CLI；
- Web 搜索、Research、Memory 审批和 CSRF 均先建立路由契约；
- T13 的干净环境测试先因缺文件、包导入和过窄文字断言失败，之后逐项修正；
- 区分“业务失败”与“测试自身过度限定实现文字”的经验。

需要回答 TDD 在 AI 协作下是阻碍还是放大器，以及在什么任务上结论不同。不要只报告测试数量。

### subagent 与任务粒度

如实说明你实际使用或未能使用的陌生智能体/子智能体。可参考 `docs/course/COLD_START_VALIDATION.md`，但必须由你判断：

- 陌生上下文在什么地方暴露了 Web 框架、错误契约、demo mode、凭据命名空间等歧义？
- 如果某一步因智能体不可用而跳过，对结论有什么限制？
- 一个任务同时包含多少文件、多少边界和多长验证最合适？
- 哪些任务可以并行，哪些因为共享配置或架构决定必须串行？

课程所问的不是“用了几个 agent”，而是你能否解释怎样让自主执行不偏离主题。

### SPEC / PLAN

至少选择一个“规约不清导致停顿或偏离”的具体案例。建议从以下真实材料中选一个你亲自经历最深的：

- 项目名称暗示开放式 Agent，但代码实际是固定五步工作流；
- 初始规格没有冻结 Flask/Jinja、`WebApplicationPort` 和 Web 错误映射；
- 环境变量读取曾被误认为完整的安全凭据生命周期；
- 本地可运行曾被误认为已经满足公开部署和分发证据；
- Linux 干净环境暴露了 Windows 本机没有暴露的模块搜索路径问题。

写清“原先怎么想—证据显示什么—你做了什么决定—最终文档或代码怎样变化”，而不是泛泛说 SPEC 很重要。

### prompt / context 策略

总结你本人认为最有效的 prompt / context 做法，可从以下问题展开：

- 为什么先让智能体读取课程要求和实际仓库，再允许宣称完成？
- 为什么早期要求只创建新文档；后来又怎样在更新活文档的同时保留历史快照和测试语料？
- 为什么把“本地通过”“远程 CI 通过”“公网部署完成”分开？
- 明确禁止虚构 URL、commit、PR 和用户测试是否改善了可靠性？
- 长任务中阶段日志、验收命令和剩余文件清单怎样帮助恢复上下文？

只写你确实使用并观察过效果的策略。

### 凭据与分发

课程的凭据与分发要求促使项目从“能运行”走向“别人能安全运行”。可说明：

- 为什么配置文件拒绝秘密还不够；
- keyring 的录入、状态、更新、删除和环境变量后备怎样形成生命周期；
- 为什么状态不能显示掩码、长度或前后缀；
- Docker 的非 root、健康检查、只读/最小复制和脱敏 demo mode解决了哪些风险；
- 干净 Linux 镜像发现了哪些本机环境依赖；
- 为什么公开 registry、远程 CI 和 HTTPS URL 在真实产生前不能写成已完成。

### 如果重做

给出 3–5 个可执行改变，而不是“更认真”。例如：

- 在第一行实现代码之前完成冷启动规格审查；
- 从第一个 task 起使用独立 worktree、短 commit 和每任务 PR；
- 先固定公开演示威胁模型，再选择 UI 与部署；
- 让 GitLab CI 从最早提交就运行 secret scan 和干净构建；
- 更早准备真实部署账户、域名/URL 和 registry；
- 将架构契约测试集中于行为，避免过度断言命令文字。

每项说明预期减少什么返工或提高什么证据质量。

## 可核验事实素材

以下是可查证材料，不代表你必须认同其评价：

- 项目最终定位：受治理的确定性 Agent Workflow，不是开放式自主 Agent；
- 固定流程：retrieve、summarize、propose、persist、writeback；
- 凭据：OS keyring 优先，显式环境变量后备，状态不回显秘密；
- WebUI：Flask/Jinja/Waitress，通过 `WebApplicationPort` 隔离业务层；
- 公开演示：固定脱敏 Vault、fake Provider、临时数据、无凭据管理；
- 分发：GitHub Release `v0.1.0` 已发布，包含源码、wheel、source distribution 和 SHA-256；Docker 与 Python 包构建用于复现；
- Release 基线：228 项测试通过，干净 Linux 验证通过，工作区与 Git 历史秘密扫描均为 0；
- 远程 CI：GitHub Actions 的 Linux Python 3.9/3.11/3.13 和 Windows 3.11 全部通过；GitLab 仅有配置契约；
- 交付：Draft PR #1 已存在，但仍缺真实人工 review/批准/合并证据；
- 部署：没有公开 WebUI；教师确认可提交 Release 链接并填写 `is_deployed=false`；
- 仍缺：学生本人 `REFLECTION.md`、最终人工评审记录和最终验收文件；
- 开发过程按用户要求忽略 Superpowers；早期以版本化新文档保留历史，后期按用户新要求统一更新活文档，并继续保护历史证据和测试语料。

建议优先查阅：`SPEC_PROCESS.md`、`docs/course/COLD_START_VALIDATION.md`、`docs/course/GITHUB_RELEASE_EVIDENCE.md`、`AGENT_LOG.md` 至 `AGENT_LOG_v12.md`、`docs/course/CLEAN_MACHINE_VERIFICATION.md` 和 `README_COURSE.md`。

## 写作自查

提交前逐项确认：

- [ ] 全文 1500–2500 字；
- [ ] 第一人称观点确实属于学生本人；
- [ ] 覆盖 Superpowers、TDD、subagent、SPEC / PLAN、prompt / context、凭据与分发、如果重做；
- [ ] 至少包含两个具体事件及其前后变化；
- [ ] 明确说明未使用 Superpowers 的偏离，不伪造技能调用；
- [ ] 准确区分：GitHub Actions/Release/PR 已存在，公开 WebUI、人工 review 和 GitLab pipeline 不存在；
- [ ] 不包含 API key、个人 Vault 内容、绝对私人路径或平台秘密；
- [ ] 若使用 AI 润色，按课程规则标明只润色了哪些内容；
- [ ] 运行字数检查并人工朗读，删除空泛套话；
- [ ] 最终由学生本人签字确认事实和观点。

可使用下面的本地字数近似检查，但最终以课程口径和人工检查为准：

```powershell
$text = Get-Content -Raw -Encoding UTF8 REFLECTION.md
($text -replace '\s','').Length
```
