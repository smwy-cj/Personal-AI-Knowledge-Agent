# Personal AI Knowledge Agent 项目反思

## 1. 项目选择与个人责任

我选择个人知识 Agent，而不是普通问答页面，是因为我希望系统不仅回答一次问题，还能围绕个人知识形成稳定的输入、处理、检索和输出流程。普通问答更像一次性交互，而个人知识管理真正困难的是资料分散、组织方式不统一，以及已有内容难以持续复用。

项目中我确认了几项关键决定：把系统从“开放式自主 Agent”收缩为固定五步 Workflow；Web 端采用 Flask/Jinja，并明确端口、错误映射和 demo mode；把凭据管理纳入系统边界。相比堆叠功能，我更重视引用可回查、记忆写入需审批，以及任务可验证。

## 2. SPEC / PLAN 如何影响实现

项目名称容易让人误解为具有自主规划能力的 Agent，但课程项目更需要明确边界和可验收结果。因此我把 Research 收敛为 retrieve、summarize、propose、persist、writeback 五步，并通过 SPEC 区分模型与确定性代码的责任。规格不是形式文档，而是在限制实现空间，防止 AI 根据模糊描述自行扩展需求。

冷启动评审暴露了框架、端口、错误映射、demo mode、keyring 命名空间和凭据优先级等歧义。后来在 `SPEC_v2.md` 中固定这些规则，返工明显减少。PLAN 也明确了依赖：凭据抽象先于 Provider 接入，Web 基础先于业务页面，分发则等待安全边界稳定。

## 3. TDD 的真实作用

TDD 最大的价值不是测试数量，而是提前暴露设计缺口。T14 文档契约最初三个目标文件都不存在，3 项测试全部失败；创建并校准文档后才转绿。只有先确认红灯对应真实要求，再修正实现，测试通过才有意义。

干净 Linux 环境又暴露了 Windows 本机掩盖的 `scripts` 和 `tests` 模块路径问题。GitHub Actions 还因为使用 `pip install --no-deps .` 而缺少 Flask；修复方式不是绕过 Web 测试，而是让 CI 安装完整依赖。这说明本地成功不能代替可复现验证。

我也遇到过测试过度绑定命令文字的问题：业务行为正确，文本变化仍会失败。后来我更倾向验证输入、输出、状态和错误边界。对 AI 协作而言，边界清晰时 TDD 是放大器；探索阶段过早固定细节则会限制调整。

## 4. subagent、上下文和任务粒度

我逐渐发现，是否使用 subagent 不是重点，重点是任务边界是否清楚。本项目结合冷启动评审、独立任务和分阶段验证降低上下文干扰。文档核对、秘密扫描和环境测试可以拆开；规格、接口、实现与测试联动的任务更适合串行，否则容易产生冲突。

我认为一次任务最好只有一个主要边界，并带有停止条件和验收命令。阶段日志与剩余清单把“之前在想什么”转换成“完成了什么、还差什么、怎样证明”，比依赖长聊天记录可靠。

## 5. Prompt / context 策略

后来我要求在宣称完成前先读取课程文件、规格和仓库状态。AI 容易补出看似合理的答案，但验收关心实际存在的内容。我一度要求只新建文档，避免不可逆的信息损失；后期更新活文档时，仍保留阶段证据、旧日志和测试语料。

我还区分“本地通过”“远程 CI 通过”“Release 已发布”和“WebUI 已部署”，因为它们不能互相替代。完成声明必须附带测试输出、CI、commit、tag 或 Release。当前有 Release 和远程 Actions，但没有公开 WebUI，因此应填写 `is_deployed=false`。

## 6. 凭据与分发

配置文件中没有 API key 并不等于完成凭据治理。还要考虑录入、更新、删除、读取优先级和状态泄露。keyring 的 set、status、update、delete 让我把凭据看成有生命周期的对象。状态只需回答来源和是否可用，不必暴露掩码、长度或前后缀。

Docker 证明容器可运行，干净 Linux 证明项目不依赖本机状态，wheel 证明包可安装，Release 证明版本形成可追踪产物。Actions 的依赖失败让我认识到，“源码能运行”不等于“用户安装后能运行”，依赖声明也是发布契约。

## 7. 对 Superpowers 的看法

本项目没有使用 Superpowers，主要因为大部分实现通过 Codex 完成，而 Codex 已能提供任务拆分、测试和记录等相近能力。我不能据此断言 Superpowers 没有价值，因为缺少实际比较。没有使用也使项目缺少相应技能调用、标准 worktree 和两阶段评审记录。

部分工程纪律通过替代流程保留下来，例如先写 SPEC/PLAN、拆小任务、用测试和验收命令证明结果。如果重做，我会在需求冻结、高风险实现和发布前验证三个阶段尝试 Superpowers，再根据返工率和证据质量决定是否保留。

## 8. 如果重做

如果重做，我会进行四项调整：更早冻结 Web 威胁模型和凭据边界；第一天就启用远程 CI；让每个任务保持单一边界并独立提交；更早准备 Release 或部署所需账户、权限和验收步骤。这些调整分别减少安全返工、环境差异、回归定位成本和交付阶段的不确定性。

这次项目让我重新理解了 AI 辅助开发中的控制问题。AI 可以快速生成代码、测试和文档，但规格、验证和完成定义不清楚时，它也会更快地产生不一致。我后来更重视三个问题：边界是什么、怎样验证、有什么真实证据。SPEC、TDD、上下文管理和发布验证最终都在约束 AI 协作，使结果可验证、可复现。

## AI 辅助说明

本文观点、经历和初稿由本人提供；AI 根据初稿进行了结构整理、语言润色和项目事实核对，未新增个人经历。
