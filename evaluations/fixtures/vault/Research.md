# Evidence-backed Research

Research retrieval produces an evidence pack with stable chunk identifiers, source paths, line numbers, and content hashes. Citation verification checks the current index and detects source drift.

The model returns structured paragraphs with citation identifiers. Markdown citation markers are rendered deterministically after validation.
