# Agent Architecture

The runtime stores durable task checkpoints after each completed workflow step. A task can resume from the latest verified checkpoint instead of repeating completed work.

Research summaries must reference evidence retrieved from the local knowledge index. Citation verification rejects missing or stale sources before memory is proposed.
