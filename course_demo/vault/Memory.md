# Governed Memory

New semantic memory is never written automatically when human approval is required. The task pauses in WAITING_USER until every candidate is explicitly approved or rejected.

Approved memory is written only to the configured managed directory inside the demo Vault. Rejected candidates remain auditable but do not become long-term memory.
