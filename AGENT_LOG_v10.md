# Agent Log v10 — 干净环境与秘密扫描

日期：2026-08-14

## 本轮目标

完成 T13：建立可重复的秘密扫描，使用独立 Linux 镜像复验项目，并留下可审计证据。按用户要求未使用 Superpowers，也未修改原始课程要求文档。

## 新增与调整

- 新增无第三方依赖的 `scripts/scan_secrets.py`；
- 新增 `Dockerfile.verify`，从 Python 3.12 slim 环境安装并执行完整验收；
- CI 在单元测试前执行秘密扫描；
- 增加 7 项扫描和交付契约测试；
- 补充跨平台包标记与验证入口路径，使 Windows 直接执行和 Linux 模块执行一致；
- 新建干净环境、秘密扫描与剩余文件盘点证据。

## 最终证据

- 当前工作区 + 10 个 Git 提交历史：0 个秘密发现；
- Windows 一键验证：225/225 通过；
- Linux 干净镜像：225/225 通过；
- Linux 镜像内扫描：0 个发现；
- `git diff --check`：通过，仅有 Windows 行尾转换提示，无空白错误。

## 迭代中发现的问题

第一次镜像复验因构建上下文缺少验证材料而失败；第二类失败来自不同平台的 Python 模块搜索路径；最后两次失败属于契约测试仅接受旧命令文字。逐项补齐上下文、统一模块调用并让契约接受语义等价形式后，完整复验通过。未隐藏失败过程，也未把中间失败计作通过。

## 下一阶段

剩余 7 个核心文件。下一步可先完成 `README_COURSE.md`、`REFLECTION_GUIDE.md` 和 `docs/course/ARCHITECTURE.md`；部署、PR/MR、远程 CI 和学生反思仍需真实外部事实或学生本人输入后才能收口。

