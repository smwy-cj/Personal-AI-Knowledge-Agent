# 规格与计划形成过程记录

状态：进行中  
开始日期：2026-08-13  
证据原则：只记录真实发生且可核验的过程；不补造历史对话、TDD 红灯、PR 或人工评审。

## 1. 输入材料

本轮课程规格整理使用了以下输入：

- AI4SE 期末项目通用要求；
- AI4SE B 类应用项目要求；
- 当前仓库源代码、测试、配置示例和 CI 配置；
- 当前 Git 分支与 commit 历史；
- 原有 README、实施计划和状态文档，均作为只读参考；
- 用户明确要求忽略 Superpowers，并要求后续只创建新文档、不修改原文档。

## 2. 第一次迭代：从“继续加功能”转向“补齐交付闭环”

### 初始观察

项目已经具备较多工程功能，包括知识摄取、检索、Research、记忆治理、执行控制、Provider 治理、观测和评测。自动化验证包含 149 项测试并在本地通过。

### 关键问题

如果继续增加 Provider、计费或评测功能，是否能提高课程验收完成度？

### 决策

不能。当前主要风险不是代码规模不足，而是缺少课程指定文档、完整凭据生命周期、WebUI、正式分发、GitLab CI、公开 URL 和过程证据。

### 产生的修订

新增 `COURSE_COMPLETION_PLAN.md`，将优先级固定为：

```text
课程硬性要求 > 安全与可运行性 > 演示闭环 > 工程证据 > 新功能数量
```

## 3. 第二次迭代：校正 Agent 定位

### 初始风险

项目名称和架构容易让评审者理解为模型可以自主规划、自由选择工具并多轮自我修正。

### 代码核验

`ApplicationService.run_research` 创建固定五步计划：

```text
retrieve → summarize → propose → persist → writeback
```

Orchestrator 自研实现依赖调度、状态、预算、重试、Checkpoint、验证、暂停、恢复和取消，但当前没有动态 Planner 或开放式工具选择。

### 决策

课程定位改为“包含受治理确定性 Agent Workflow 的 B 类应用”，不宣称完全自主 Agent。

### 产生的修订

`SPEC.md` 增加 Agent 边界章节，明确：

- 模型负责受约束的摘要推理；
- 计划当前由应用代码预定义；
- 调度、治理和验证由项目自身实现；
- 去除真实 LLM 后，机制仍可使用 fake workflow 和 fake Provider 测试。

## 4. 第三次迭代：区分“已有安全基础”与“完整凭据生命周期”

### 初始观察

现有配置拒绝密钥字段，Provider 在调用时从环境变量读取凭据，带凭据的远程请求强制 HTTPS，并已有脱敏错误测试。

### 关键问题

这些能力是否足以满足课程要求的安全存储、首次录入、查看、更新和清除？

### 决策

不足。环境变量只是当前凭据来源，并不构成完整的安全持久化和生命周期管理。

### 产生的修订

`SPEC.md` 将以下内容标为待实现：

- OS keyring；
- 隐藏录入；
- 状态查询但不显示明文；
- 覆盖更新；
- 清除；
- keyring 与环境变量的明确优先级。

`PLAN.md` 将其拆为 T4 存储抽象和 T5 CLI/Provider 接入两个任务。

## 5. 第四次迭代：保护原有文档

### 用户约束

用户要求之后均生成新的文档，不在原来的文档基础上修改。

### 决策

- 原 README 和 `docs/` 内已有文档作为只读历史材料；
- 课程入口使用新建 `README_COURSE.md`；
- 后续规格修订使用 `SPEC_v2.md` 等新版本；
- 过程记录不通过改写历史来制造合规证据。

### 影响

实现任务若必须调整现有业务代码仍可修改代码；“只新增不修改”约束目前明确适用于文档。若用户希望连代码文件也禁止修改，需要重新评估技术可行性。

## 6. 被采纳、修改和拒绝的建议

### 已采纳

- 收束功能范围，优先补齐课程交付闭环；
- 使用 WebUI 展示搜索、Research 和 Memory 审批；
- 使用系统密钥环实现安全凭据生命周期；
- 使用 Docker 作为主分发形态；
- 为 GitLab 增加名为 `unit-test` 的 job；
- 将线上演示限制为脱敏示例 Vault。

### 修改后采纳

- “项目是 Agent”被修改为“受治理的确定性 Agent Workflow”，避免超出代码事实。
- “发布 Python 包”被调整为 Docker 主路线、Python 包辅助路线，因为最终还需要 WebUI 部署。
- “补写历史过程文档”被调整为有证据的回溯和从现在开始的真实记录。

### 未采纳

- 继续扩展更多 Provider、账单格式或多 Agent：与当前课程硬性缺口无直接关系。
- 直接改写原 README：违反用户的文档保护要求。
- 由 AI 代写最终 `REFLECTION.md`：课程明确要求学生本人完成。

## 7. 冷启动验证

状态：尚未执行。

计划在正式实现凭据管理和 WebUI 前，由不同类型的全新智能体仅凭 `SPEC.md` 与 `PLAN.md` 评审 T4/T6。执行结果将记录在新建的 `docs/course/COLD_START_VALIDATION.md` 中。

不得在验证发生前填写以下内容：

- 智能体提出的问题；
- 暴露的规格缺陷；
- 代码产出；
- 修订 diff；
- 运行时间和停止点。

## 8. 当前未决问题

1. Web 框架最终选型。
2. 公网演示是否完全禁用真实 Provider，还是允许平台 secret 配置测试账户。
3. 公网演示是否需要登录；最低方案是演示模式禁用危险写入并使用临时数据。
4. OS keyring 在 Linux 容器中的行为与安全后备方案。
5. 原文档只读约束是否允许修改 `pyproject.toml` 等非文档配置；当前按“允许实现所需代码/配置修改”理解。

## 9. 对当前规格过程的初步反思

做得较好的地方：

- 先核验代码和测试，再声明已有能力；
- 把本地证据和远程未验证状态分开；
- 没有把项目名称直接等同于完整自主 Agent；
- 明确把 WebUI、keyring 和发布写为未来工作。

仍需改进的地方：

- 当前规格尚未经陌生智能体冷启动验证；
- Web 身份验证和容器内 keyring 的边界仍有歧义；
- 规格的部分性能目标需要在最终部署环境重新校准；
- 真实用户验收尚未发生。
